/:
  controller: main
  action: home




/nous-contacter:
  controller: main
  action: contact

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/dashboard:
  controller: admin
  action: home
  security: true

/accueil:
  controller: main
  action: home

/recuperer_mdp:
  controller : user
  action     : recuperer_mdp

/initialiser_mdp:
  controller : user
  action     : initialiser_mdp